import {StyleSheet, View} from 'react-native';

export const Dashboard = () => {
  return <View style={styles.takingAllPage}></View>;
};

const styles = StyleSheet.create({
  takingAllPage: {flex: 1},
});

export default Dashboard;
